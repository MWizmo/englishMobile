<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.englishapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EnglishApp"
        android:usesCleartextTraffic="true">
        <activity android:name=".activities.TheoryActivity"></activity>
        <activity android:name=".activities.TaskActivity" />
        <activity android:name=".activities.SectionActivity" />
        <activity android:name=".activities.SectionsListActivity" />
        <activity android:name=".activities.ModulesActivity" />
        <activity android:name=".activities.DictionaryActivity" />
        <activity android:name=".activities.WordCardActivity" />
        <activity android:name=".activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>